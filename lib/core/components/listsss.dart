import 'package:flutter/cupertino.dart';

class listss extends StatelessWidget {
  const listss({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ListView();
  }
}
